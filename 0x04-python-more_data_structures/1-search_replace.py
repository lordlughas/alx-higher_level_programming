#!/usr/bin/python3

def search_replace(my_list, search, replace):
    """
    search and replace word, string or integer
    """
new_list = my_list[:]
    for idx in range(len(update_list)):
        if new_list[idx] == search:
            new_list[idx] = replace
    return (new_list)
